apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: scrolller-web-hpa
  namespace: scrolller
  labels:
    app: scrolller-web
spec:
  minReplicas: 1
  maxReplicas: 3
  metrics:
    - resource:
        name: cpu
        target:
          averageUtilization: 60
          type: Utilization
      type: Resource
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: scrolller-web-deployment
